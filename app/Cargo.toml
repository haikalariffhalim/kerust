[package]
name = "app"
version = "0.1.0"
edition = "2021"

[dependencies]
data_scraper = { path = "../scrub" }
markdown_parser = { path = "../markdown" }
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
clap = { version = "4", features = ["derive"] }
anyhow = "1.0"
